language: cpp
compiler:
- clang
- gcc
os:
- linux
script:
  - bash ./build.sh
addons:
  apt:
    packages:
      - cmake
      - gcc
      - clang
      - libgflags-dev
      - libgflags2
      - libgoogle-glog-dev
      - valgrind
    sources:
      - kalakris-cmake