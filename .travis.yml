language: cpp
compiler:
- clang
- gcc
os:
- linux
script:
  - bash ./build.sh
addons:
  apt:
    packages:
      - cmake
      - gcc
      - clang
      - libgflags-dev
      - libgoogle-glog-dev
      - valgrind
    sources:
      - kalakris-cmake