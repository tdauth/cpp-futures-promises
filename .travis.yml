language: cpp
compiler: clang
os: linux
script:
  - bash ./build.sh
addons:
  apt:
    packages:
      - cmake
      - libgflags-dev
      - libgflags2
      - libgoogle-glog-dev
      - valgrind
      - clang
    sources:
      - kalakris-cmake